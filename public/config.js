window.config = {
    assetmgmt: {
        columns: [
            {key: 'hostname', label: 'Hostname'},
            {key: 'port', label : 'Port'}
        ]
    },

    uiSchema: {
        assetProperties: {
            properties: {
            }
        }
    }
};
